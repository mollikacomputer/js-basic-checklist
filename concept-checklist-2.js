// 1. Write 3 variables (Number, String, Boolean)

  // Number Variable
//   var studentAge = 24;
  // String Variable
//   var StudentName = 'Shamal Kanti Roy';
  // Boolean Variable 

/*   var areYouMarriade = 1;
  var x = 0;
  console.log(Boolean(x));
  console.log(Boolean(areYouMarriade)); */

// 2. diclare 2 variables (let, const), if Changable variable you change some variable 
     /*   let studentName = "Ramjan Ali";
       let studentCertificateName = "Ramjan Ali Matubber";
        studentName = studentCertificateName;
       const schoolName = "Amgram High School";
       console.log(studentName); */

// 3. simple math operations () +,  -, *, /, %)
    // math operator +
   /*  function add(number1, number2){
            var gogFol = number1 + number2;
            return  gogFol;
    }
    const twoNumberGogFol = add(353, 952);
    console.log( 'Two number jog Fol is :', twoNumberGogFol);
 */
    // math operator -

    // function subtraction(firstNumber, secondNumber){
    //     var biogFol = firstNumber - secondNumber;
    //     return biogFol;
    // }
    // var biogFolResult = subtraction(253, 43);
    // console.log('BiogFol is :', biogFolResult);
    
    // Multiplication *
    
    // function getMultiplication(num1, num2){
    //     var multiply = num1 * num2;
    //     return multiply;
    // }
    // var gunFol = getMultiplication(25, 28);
    // console.log('Two number gunFol is:', gunFol);

       //  Division
   /*  function getDivision(num1, num2){
        var vag = num1 / num2;
        return vag;
    }
    var vagFol = getDivision(350, 10);
    console.log('VagFol is :', vagFol); */
// %	Modulus (Remainder)
/* 
function getModulas(num1, num2){
    var modulas = num1 % num2;
    return modulas;
}
const modulasResult = getModulas(919, 5);
console.log('Get modulas Result:', modulasResult);
 */
// 5. Two conditions, 
     /*    let ages = [99, 17, 25, 18, 29, 30, 16, 91,12,  82, 50];
       for(let age of ages){
           
            if(age == 30){
                console.log("Your age is 30");
            }else if(age==50){
                    console.log('You are 50');
            }else if(age >=80){
                console.log('Your age is over 80 or equal');
            } 
       }
 */
// 6. if else use
// same as 5

// 7. while loop to display 7 to 19 all numbers. only display odd numbers including 7 to 19
    let mangoes = [25, 30, 23, 33, 39];
    let i = 0;
    let mango = ' ';
    while( i < mangoes.length){
        mango +=  mangoes[i]  ;
        i++;
    }
    console.log(mango);
// 8. declare an array number of elements. update or change 4th positions element. 
// add or remove element. check whether existing specific value in the arrey.



// 9. use any for loop to display every elements of an array

// 10. you have an array of numbers. display only the numbers bigger than 80;

//11. write a function three numbers and return the muntiplication of the three numbers
// 12. declare an object change any property of that 

